import React from 'react';
import PropTypes from 'prop-types';

CartNotify.propTypes = {
    
};

function CartNotify(props) {
    return (
        <div>
            
        </div>
    );
}

export default CartNotify;